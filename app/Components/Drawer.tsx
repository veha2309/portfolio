
// import { animate } from "animejs";
// import { useRef } from "react";

const Drawer = () => {
    // const drawerRef = useRef<HTMLDivElement>(null);
    // if(drawerRef.current === null) return null;
    // const controller = animate(drawerRef.current, {
        
    // })
    return (
        <>
        <div className={`
           h-full 
           w-[60%] 
           bg-gray-200 
           absolute 
           top-0 right-full
           z-20
           `}>
                
        </div>
        </>
    )
};

export default Drawer;